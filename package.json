{
  "name": "herman-freiman",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0 --port 8080",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0 --port 8080"
  },
  "dependencies": {
    "astro": "^4.16.0"
  }
}
